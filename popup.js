(async()=>{const defaults={enabled:true,mode:'blackout',blockedChannels:[]};const data=await chrome.storage.local.get(defaults);const enabled=document.querySelector('#enabled');const mode=document.querySelector('#mode');const count=document.querySelector('#count');enabled.checked=data.enabled;mode.value=data.mode;count.textContent=data.blockedChannels.length;enabled.addEventListener('change',()=>chrome.storage.local.set({enabled:enabled.checked}));mode.addEventListener('change',()=>chrome.storage.local.set({mode:mode.value}));document.querySelector('#options').addEventListener('click',()=>chrome.runtime.openOptionsPage());})();
